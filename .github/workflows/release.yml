# Automatically create GitHub releases with build artifacts
# for every push to the mc26.1 branch

name: Release

on:
  push:
    branches:
      - mc26.1

jobs:
  build:
    name: Build Artifacts
    runs-on: ubuntu-24.04
    outputs:
      mod_version: ${{ steps.version.outputs.mod_version }}
      minecraft_version: ${{ steps.version.outputs.minecraft_version }}
      minecraft_range: ${{ steps.version.outputs.minecraft_range }}
      minecraft_range_neoforge: ${{ steps.version.outputs.minecraft_range_neoforge }}
      fabric_version: ${{ steps.version.outputs.fabric_version }}
      loader_version: ${{ steps.version.outputs.loader_version }}
      neoforge_version: ${{ steps.version.outputs.neoforge_version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract version information
        id: version
        run: |
          VERSION=$(grep '^mod_version=' gradle.properties | cut -d'=' -f2)
          MC_VERSION=$(grep '^minecraft_version=' gradle.properties | cut -d'=' -f2)
          FABRIC_VERSION=$(grep '^fabric_api_version=' gradle.properties | cut -d'=' -f2)
          LOADER_VERSION=$(grep '^loader_version=' gradle.properties | cut -d'=' -f2)
          NEOFORGE_VERSION=$(grep '^neoforge_version=' gradle.properties | cut -d'=' -f2)
          echo "mod_version=$VERSION" >> $GITHUB_OUTPUT
          echo "minecraft_version=$MC_VERSION" >> $GITHUB_OUTPUT
          MC_RANGE=$(grep '^minecraft_range=' gradle.properties | cut -d'=' -f2)
          MC_RANGE_NEO=$(grep '^minecraft_range_neoforge=' gradle.properties | cut -d'=' -f2)
          # Branches ohne eigene NeoForge-Range nutzen weiterhin die gemeinsame
          MC_RANGE_NEO=${MC_RANGE_NEO:-$MC_RANGE}
          echo "minecraft_range=$MC_RANGE" >> $GITHUB_OUTPUT
          echo "minecraft_range_neoforge=$MC_RANGE_NEO" >> $GITHUB_OUTPUT
          echo "fabric_version=$FABRIC_VERSION" >> $GITHUB_OUTPUT
          echo "loader_version=$LOADER_VERSION" >> $GITHUB_OUTPUT
          echo "neoforge_version=$NEOFORGE_VERSION" >> $GITHUB_OUTPUT
          echo "Extracted versions: mod=$VERSION, minecraft=$MC_VERSION, fabric=$FABRIC_VERSION, loader=$LOADER_VERSION, neoforge=$NEOFORGE_VERSION"

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Setup JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'microsoft'

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ steps.version.outputs.mod_version }}
          path: |
            fabric/build/libs/
            neoforge/build/libs/
          retention-days: 7

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-${{ needs.build.outputs.mod_version }}
          path: .

      - name: Extract changelog for version
        id: changelog
        run: |
          VERSION="${{ needs.build.outputs.mod_version }}"
          # Extract section between [VERSION] and next [VERSION] or end
          # Using awk for more reliable multi-line extraction
          CHANGELOG=$(awk "/## \[$VERSION\]/{flag=1; next} /## \[.*\]/{flag=0} flag" CHANGELOG.md | sed '/^$/d')

          if [ -z "$CHANGELOG" ]; then
            echo "Warning: No CHANGELOG entry found for version $VERSION"
            CHANGELOG="No changelog entry available for this version."
          fi

          {
            echo 'changelog<<EOF'
            echo "$CHANGELOG"
            echo 'EOF'
          } >> $GITHUB_OUTPUT

      - name: Get commits since last release
        id: commits
        run: |
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            echo "No previous tag found, including all commits"
            COMMITS=$(git log --pretty=format:"- %s (%h)" --no-merges | head -20)
          else
            echo "Last tag: $LAST_TAG"
            COMMITS=$(git log $LAST_TAG..HEAD --pretty=format:"- %s (%h)" --no-merges)
          fi

          {
            echo 'commits<<EOF'
            echo "$COMMITS"
            echo 'EOF'
          } >> $GITHUB_OUTPUT

      - name: Generate release body
        run: |
          cat << 'RELEASE_BODY' > release_body.md
          ## What's New in v${{ needs.build.outputs.mod_version }}

          ${{ steps.changelog.outputs.changelog }}

          ## Compatibility

          - **Minecraft:** ${{ needs.build.outputs.minecraft_version }}
          - **Fabric:** Fabric Loader ${{ needs.build.outputs.loader_version }}+, Fabric API ${{ needs.build.outputs.fabric_version }}, YetAnotherConfigLib
          - **NeoForge:** ${{ needs.build.outputs.neoforge_version }}+, YetAnotherConfigLib
          - **Java:** 25+

          ## Build Artifacts

          - `auto_shulker_inventory-fabric-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range }}.jar` - Fabric mod file
          - `auto_shulker_inventory-neoforge-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range_neoforge }}.jar` - NeoForge mod file
          - `*-sources.jar` - Source code

          ## Recent Commits

          ${{ steps.commits.outputs.commits }}

          ---

          **Installation:** Download the JAR matching your mod loader and place it in your mods folder. On Fabric you also need Fabric API and YetAnotherConfigLib; on NeoForge you need YetAnotherConfigLib.

          For detailed installation instructions, see the [README](https://github.com/DennisTheGamer/auto-shulker-inventory/blob/mc26.1/README.md).
          RELEASE_BODY

      - name: Verify release artifacts
        # Ohne diese Pruefung legt der Job ein LEERES Release an: die
        # Release-Action uebergeht fehlende Dateien stillschweigend und meldet
        # trotzdem Erfolg. Genau so blieben 1.5.0 und 1.5.1 hier ohne Jars.
        env:
          EXPECTED: |
            fabric/build/libs/auto_shulker_inventory-fabric-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range }}.jar
            fabric/build/libs/auto_shulker_inventory-fabric-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range }}-sources.jar
            neoforge/build/libs/auto_shulker_inventory-neoforge-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range_neoforge }}.jar
            neoforge/build/libs/auto_shulker_inventory-neoforge-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range_neoforge }}-sources.jar
        run: |
          set -eu
          missing=0
          while IFS= read -r f; do
            # bewusst als if, nicht als &&-Liste -- die loest unter bash -e aus
            if [ -z "$f" ]; then
              continue
            fi
            if [ ! -f "$f" ]; then
              echo "::error::Release-Artefakt fehlt: $f"
              missing=1
            fi
          done <<< "$EXPECTED"
          if [ "$missing" -ne 0 ]; then
            echo "Tatsaechlich gebaut:"
            find fabric/build/libs neoforge/build/libs -name "*.jar" | sed "s/^/  /"
            exit 1
          fi
          echo "Alle vier Release-Artefakte vorhanden."

      - name: Create Release
        uses: andelf/nightly-release@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ needs.build.outputs.mod_version }}-mc26.1
          name: Auto Shulker Inventory Loader v${{ needs.build.outputs.mod_version }} (MC 26.1)
          prerelease: false
          body_path: release_body.md
          files: |
            fabric/build/libs/auto_shulker_inventory-fabric-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range }}.jar
            fabric/build/libs/auto_shulker_inventory-fabric-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range }}-sources.jar
            neoforge/build/libs/auto_shulker_inventory-neoforge-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range_neoforge }}.jar
            neoforge/build/libs/auto_shulker_inventory-neoforge-${{ needs.build.outputs.mod_version }}+mc${{ needs.build.outputs.minecraft_range_neoforge }}-sources.jar
