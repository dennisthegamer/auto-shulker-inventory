architectury { common('fabric', 'neoforge') }

dependencies {
    // compile-time only: @Environment + Mixin lib. Never CALL loader classes from common.
    modImplementation "net.fabricmc:fabric-loader:${rootProject.loader_version}"
    // compile against the fabric variant for the API; both loader variants share dev.isxander.yacl3.*
    modCompileOnly "dev.isxander:yet-another-config-lib:${rootProject.yacl_version}-fabric"
}
